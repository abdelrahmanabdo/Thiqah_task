import { AllowedFileTypes } from '../enums/files.enum';

export const FILE_PATH_EXPIRATION_TIME = 1000_000;
export const ALLOWED_FILE_TYPES = `${AllowedFileTypes.JPEG}|${AllowedFileTypes.PNG}|${AllowedFileTypes.PDF}}`;
